name: Test Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # allows manual triggering

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      
      - name: Run a test script
        run: |
          echo "Hello from GitHub Actions!"
          python -c "print('This is a dummy test run')"
